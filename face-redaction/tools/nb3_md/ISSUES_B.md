# ISSUES_B: findings on the code cells of the second half (nothing was edited; the coordinator decides)

All figures below are from `data/nb3/nb3_codeonly.ipynb` (cells=29, errors=0). No defect blocks the notebook; items 1-4 are things the markdown now states plainly.

1. **CHECK 4.2a, 4.2b, 4.5 are honest FAILs** (`20_crc.py`). The a-priori claim "CRC's per-split violation exceeds delta + 3 SE = 0.152" was not met at the main size (35/300 = 0.117; sweep max 0.12). Not a code defect; the claim was wrong. The markdown reports it and points to CHECK 4.7 (small calibration sets) and 4.4 (face-free dilution) as where the gap is real. If the coordinator wants the checks green, the claim (not the threshold) should be rewritten, e.g. "violation exceeds delta at alpha = 0.05" and be labelled as a within-tolerance result.
2. **CHECK 5.5 FAIL** (`23_thm19.py`): 1 instance-level nesting violation (0 pool-level). Reported as "approximately nested".
3. **CHECK 6.3 passes at the threshold**: max/min = 4.635/2.289 = 2.025 against a threshold of 2.0 (`27_cost.py`). Borderline; called so in the markdown.
4. **CHECK 7.5 mislabelled** (`28_reverify.py`): the text says the small-set client verdict is INCONCLUSIVE, the harness returns FAIL (4 observed misses already reject 99%, precision far below 0.95); the check passes because the verdict is "not PASS". Coordinator said they will reword; markdown describes the real behaviour.
5. **Chart omission, `20b_crc_viz.py` top-left panel**: LTT-1D's flat ~0.003 violation line looks like a free win but is bought with abstention that this panel does not show (abstain 1.00 / 0.99 / 0.70 at alpha 0.03 / 0.04 / 0.05). Suggested fix: overlay LTT-1D's abstain rate as a dotted line, as the top-right panel already does. The bottom-left/right bars average over non-abstaining runs only (LTT-1D on the fine ladder: about 30% of runs; LTT-full: 76%); suggest annotating n on each bar.
6. **Cosmetics** (charts): `27b_cost_viz.py` middle panel: the "break-even 12x" text overlaps the legend and the "assumed 8x" label sits on the curves; `28b_reverify_viz.py` left panel: the y label is longer than the axis and is clipped at the figure edge; `26_audit_viz.py` left panel: the "client: 99%" text overlaps the curves.
7. **Stale comment, `22_excluded_pool.py`**: the comment above the dependent-segments coverage study says "each replicate audit sees only 150 episodes ... n0 = 200"; the code and the CHECK 5.4 text use E_SUB = 600 and N0_POP = 1000. Numbers in the output are right; only the comment is stale.
8. **Coverage of exactly 1.0000 in CHECK 5.3 is not evidence of sharpness** (`22_excluded_pool.py`): eta_pop = 0.0054 is below what K0 = 0 certifies at n0 = 300, so the bound cannot fail. Stated in the markdown. A sharper test would use a config with eta_pop near 0.01. Not required.
9. **Unverified brief attribution.** The brief asks for "per-stratum reporting because all guarantees are marginal [P1 Sec. 5]". Re-reads 8, 9, 10, 11, 12 contain no P1 Sec. 5 statement about marginality (Sec. 5 of P1 appears in q10 only for uniform concentration needing n ~ 1e5). The markdown attributes stratified audits to [P3 Sec. 9, facered-q11] and marks the marginal-guarantee point as derived here.
10. **Brief wording**: the brief calls `16_throughput` part of section 3 (it is, and section 6 refers back to it). No trace contradicted the code.
11. **Post-hoc observation worth the coordinator's attention** (`23_thm19.py`): nested members thr 0.40 and 0.45 have U_Q(1, 515, 0.05) = 0.0092 < 1%. The markdown and the summary cell warn that epsilon = 1% was not pre-registered for the family and that this is not a certificate (a delta/M union bound puts none of the 11 members under 1%). A fresh audit set with a pre-registered config and epsilon = 0.01 is the honest route.
